import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  list: {
    width: 250,
  },
  fullList: {
    width: 'auto',
  },
  styledDiv: {
    marginTop: '350px'
  }
});

export default useStyles;