import styled from 'styled-components';

const StyledSection = styled.section`

`;

const StyledContainer = styled.div`

`;

const StyledForm = styled.form`


`;

export { StyledSection, StyledContainer, StyledForm}
