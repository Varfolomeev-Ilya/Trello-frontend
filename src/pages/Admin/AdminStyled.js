import styled from 'styled-components';

const StyledSection = styled.div`
    width: 100%;
    height: auto;
    margin: 0 auto;
`;

const StyledButton = styled.button`
    width:100px;
    height: 50px;
`;

export { StyledSection, StyledButton }